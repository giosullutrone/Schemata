import UmlEdge from './UmlEdge';

export const edgeTypes = {
  inheritance: UmlEdge,
  implementation: UmlEdge,
  composition: UmlEdge,
  aggregation: UmlEdge,
  dependency: UmlEdge,
  association: UmlEdge,
};
